<nav class="navbar navbar-expand-lg symnumeo-navbar navbar-light">
	<div class="container-fluid">
		<a class="navbar-brand d-flex align-items-center" href="/">
			<img src="{{ asset('img/logo.png') }}" alt="" class="me-1 vihbpq">
			{{ appName }}
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor04" aria-controls="navbarColor04" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor04">
			<ul class="navbar-nav me-auto">
				{% if app.user %}
					<li class="nav-item">
						<a href="{{ path('app_user_subscription_show') }}" class="nav-link">
							💎 Mon abonnement
						</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="{{ path('app_subscription_show') }}" class="nav-link">
							💎 Abonnement praticien
						</a>
					</li>
				{% endif %}
				<li class="nav-item">
					<a href="#" class="nav-link">
						📣 Contact
					</a>
				</li>
			</ul>
			<ul class="navbar-nav q6csu5">
				{% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}
						<li class="nav-item">
							<a href="{{ path('admin') }}" class="nav-link">Administration</a>
						</li>
					{% endif %}
					{% set avatarPath = app.user.avatar ? vich_uploader_asset(app.user.avatar, 'imageFile') : null %}
					<li class="nav-item dropdown">
						<button class="dropdown-toggle btn-dropdown-user" type="button" data-bs-toggle="dropdown" data-bs-display="static" aria-expanded="false" aria-label="Menu utilisateur">
							<span class="sc-gravatar">
								{% if avatarPath %}
									<img src="{{ avatarPath|imagine_filter('avatar_admin') }}" alt="{{ app.user.fullname }}" class="sc-img-gravatar">
								{% else %}
									<span class="sc-img-gravatar sc-avatar-placeholder">{{ app.user.firstname|first|upper }}</span>
								{% endif %}
							</span>
						</button>
						<ul class="dropdown-menu dropdown-menu-lg-end py-0">
							<li class="py-2 px-2">
								<a class="dropdown-item dropdown-user color-btn-logout" href="{{ path('app_profile') }}">Profil</a>
							</li>
							<li class="py-2 px-2">
								<a href="{{ path('app_device_index') }}" class="dropdown-item dropdown-user color-btn-logout">Mes appareils</a>
							</li>
							<li>
								<div class="space-between"></div>
							</li>
							<li class="py-2 px-2">
								<a class="dropdown-item dropdown-user color-btn-logout" href="{{ path('app_logout') }}">Se déconnecter</a>
							</li>
						</ul>
					</li>
				{% else %}
					<li class="nav-item me-2">
						<a href="{{ path('app_login') }}" class="btn btn-primary btn-connect">Se connecter</a>
					</li>
				{% endif %}
			</ul>
			<div class="d-flex align-items-center" data-controller="theme">
				<button type="button" class="btn btn-link nav-link px-2" aria-label="Basculer le thème" title="Basculer le thème" data-action="theme#toggle" data-theme-target="button">
					<i class="bi bi-moon-stars" data-theme-target="icon" aria-hidden="true"></i>
				</button>
			</div>
		</div>
	</div>
</nav>
